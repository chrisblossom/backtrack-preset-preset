{
  "name": "@backtrack/preset-preset",
  "version": "0.0.2",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "engines": {
    "node": ">=6.9.0",
    "npm": ">=3.10.10",
    "yarn": ">=1.6.0"
  },
  "files": [
    "lib/"
  ],
  "main": "lib/preset-preset.js",
  "scripts": {
    "format": "backtrack format",
    "lint": "backtrack lint",
    "lint.fix": "backtrack lint.fix",
    "precommit": "lint-staged",
    "prepublishOnly": "npm run lint",
    "prepush": "npm run lint"
  },
  "lint-staged": {
    "*.{js,md,json}": [
      "prettier --write",
      "git add"
    ]
  },
  "backtrack": {
    "presets": [
      "./"
    ]
  },
  "devDependencies": {
    "@backtrack/core": "0.0.5"
  },
  "dependencies": {
    "@backtrack/preset-style": "0.0.1",
    "eslint-plugin-node": "^6.0.1"
  }
}
