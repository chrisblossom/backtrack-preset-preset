'use strict';

module.exports = {
    presets: ['./'],

    // config: {
    //     wallaby: (config) => {
    //         const files = config.files.map((file) => {
    //             if (file === 'lib/**/*.js') {
    //                 return { pattern: 'lib/**/*.js', instrument: false };
    //             }
    //
    //             return file;
    //         });
    //
    //         config.files = files;
    //
    //         return config;
    //     },
    // },
};
